<h1>Write a Review!</h1>
<h2><%= @performance.run.company.name %></h2>

<h2><%= @performance.date.strftime("%A, %B %e, %Y") %>, <%= @performance.time.strftime("%l:%M %p") %></h2>


<%= render "review_form" %>