<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br/>

  <%= f.label :photo_url %>
  <%= f.url_field :photo_url %><br/>  

  <%= f.submit "Update Profile" %>
<% end %>